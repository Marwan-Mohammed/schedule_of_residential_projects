import 'package:flutter/material.dart';

class ProtrusionL extends StatelessWidget {
  const ProtrusionL({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
      mainAxisSize: MainAxisSize.min,
      children: [
        Container(
          color: const Color(0xFF90A4AE),
          width: 80,
          height: 50,
        ),
        Container(
          color: Theme.of(context).primaryColor,
          width: 80,
          height: 20,
        ),
      ],
    );
  }
}

class ProtrusionW extends StatelessWidget {
  const ProtrusionW({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
      mainAxisSize: MainAxisSize.min,
      children: [
        Container(
          color: const Color(0xFF90A4AE),
          width: 80,
          height: 50,
        ),
        Container(
          color: Theme.of(context).primaryColor,
          width: 20,
          height: 50,
        ),
      ],
    );
  }
}

class ProtrusionLL extends StatelessWidget {
  const ProtrusionLL({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
      mainAxisSize: MainAxisSize.min,
      children: [
        Container(
          color: Theme.of(context).primaryColor,
          width: 80,
          height: 20,
        ),
        Container(
          color: const Color(0xFF90A4AE),
          width: 80,
          height: 50,
        ),
        Container(
          color: Theme.of(context).primaryColor,
          width: 80,
          height: 20,
        ),
      ],
    );
  }
}

class ProtrusionWW extends StatelessWidget {
  const ProtrusionWW({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
      mainAxisSize: MainAxisSize.min,
      children: [
        Container(
          color: Theme.of(context).primaryColor,
          width: 20,
          height: 50,
        ),
        Container(
          color: const Color(0xFF90A4AE),
          width: 80,
          height: 50,
        ),
        Container(
          color: Theme.of(context).primaryColor,
          width: 20,
          height: 50,
        ),
      ],
    );
  }
}

class ProtrusionWL extends StatelessWidget {
  const ProtrusionWL({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisSize: MainAxisSize.min,
          children: [
            Container(
              color: const Color(0xFF90A4AE),
              width: 80,
              height: 50,
            ),
            Container(
              color: Theme.of(context).primaryColor,
              width: 20,
              height: 50,
            ),
          ],
        ),
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Container(
              color: Theme.of(context).primaryColor,
              width: 80,
              height: 20,
            ),
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
          ],
        ),
      ],
    );
  }
}

class ProtrusionLLW extends StatelessWidget {
  const ProtrusionLLW({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
      mainAxisSize: MainAxisSize.min,
      children: [
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisSize: MainAxisSize.min,
          children: [
            Container(
              color: Theme.of(context).primaryColor,
              width: 80,
              height: 20,
            ),
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
          ],
        ),
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Container(
              color: const Color(0xFF90A4AE),
              width: 80,
              height: 50,
            ),
            Container(
              color: Theme.of(context).primaryColor,
              width: 20,
              height: 50,
            ),
          ],
        ),
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisSize: MainAxisSize.min,
          children: [
            Container(
              color: Theme.of(context).primaryColor,
              width: 80,
              height: 20,
            ),
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
          ],
        ),
      ],
    );
  }
}

class ProtrusionWWL extends StatelessWidget {
  const ProtrusionWWL({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
      mainAxisSize: MainAxisSize.min,
      children: [
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisSize: MainAxisSize.min,
          children: [
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
            Container(
              color: Theme.of(context).primaryColor,
              width: 80,
              height: 20,
            ),
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
          ],
        ),
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Container(
              color: Theme.of(context).primaryColor,
              width: 20,
              height: 50,
            ),
            Container(
              color: const Color(0xFF90A4AE),
              width: 80,
              height: 50,
            ),
            Container(
              color: Theme.of(context).primaryColor,
              width: 20,
              height: 50,
            ),
          ],
        ),
      ],
    );
  }
}

class ProtrusionLLWW extends StatelessWidget {
  const ProtrusionLLWW({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
      mainAxisSize: MainAxisSize.min,
      children: [
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisSize: MainAxisSize.min,
          children: [
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
            Container(
              color: Theme.of(context).primaryColor,
              width: 80,
              height: 20,
            ),
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
          ],
        ),
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Container(
              color: Theme.of(context).primaryColor,
              width: 20,
              height: 50,
            ),
            Container(
              color: const Color(0xFF90A4AE),
              width: 80,
              height: 50,
            ),
            Container(
              color: Theme.of(context).primaryColor,
              width: 20,
              height: 50,
            ),
          ],
        ),
        Row(
          mainAxisAlignment: MainAxisAlignment.start,
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisSize: MainAxisSize.min,
          children: [
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
            Container(
              color: Theme.of(context).primaryColor,
              width: 80,
              height: 20,
            ),
            Container(
              color: const Color.fromARGB(255, 189, 194, 242).withOpacity(0.8),
              width: 20,
              height: 20,
            ),
          ],
        ),
      ],
    );
  }
}
